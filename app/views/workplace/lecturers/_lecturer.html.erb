<li class='item'>
  <div class="actions">
    <%= link_to 'редактировать', edit_workplace_lecturer_path(lecturer), :class => 'yellow_button faded in_frame', 'data-parent' => '.workplace_wrapper', :remote => true %>
    <%= link_to 'удалить', workplace_lecturer_path(lecturer), :method => :delete, :confirm => 'Вы уверены?', :class => 'delete_button faded' %>
  </div>

  <%= link_to lecturer.full_name, edit_workplace_lecturer_path(lecturer), :class => 'title in_frame', 'data-parent' => '.workplace_wrapper', :remote => true %>
  <div class="hidden_value"><%= lecturer.full_name %></div>
</li>
