<%= simple_form_for [:workplace, @organization, @discipline] do |f| %>
  <%= f.input :title %>

  <div class="buttons">
    <%= f.submit "Save" %>
    <%= link_to "Cancel", :back %>
  </div>
<% end %>
