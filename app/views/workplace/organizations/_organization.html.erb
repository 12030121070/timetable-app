<%= organization.title %>

<%= link_to 'Add lecturer', new_workplace_organization_lecturer_path(organization) %>
<%= link_to 'Add building', new_workplace_organization_building_path(organization) %>
<%= link_to organization.title, workplace_organization_path(organization) %>
