<% if @organization.persisted? %>
  <div class="organization_info data_presentation">
    <%= link_to workplace_root_path do %>
      <span></span>
      <b><%= @organization.title %></b>
    <% end %>

    <%= link_to "редактировать", edit_workplace_organization_path(@organization), :remote => :true, :class => 'in_frame' if link %>
  </div>
<% end %>
