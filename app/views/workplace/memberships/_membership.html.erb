<li>
  <% if membership.activated? %>
    <%= "#{membership.user.email} (#{membership.role_text})" %>
  <% else %>
    <%= "#{membership.email} (#{membership.role_text}) not activated yet" %>
  <% end %>

  <%= link_to 'Edit', edit_workplace_organization_membership_path(membership.organization, membership) if membership.activated? %>
  <%= link_to 'Delete', workplace_organization_membership_path(membership.organization, membership), :method => :delete, :confirm => 'Are you sure?' %>
</li>
